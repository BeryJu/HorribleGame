{"fragmennt":["uniform sampler2D oceanTexture;\r","uniform sampler2D sandyTexture;\r","uniform sampler2D grassTexture;\r","uniform sampler2D rockyTexture;\r","uniform sampler2D snowyTexture;\r","varying vec2 vUV;\r","varying float vAmount;\r","void main() {\r","\tvec4 water = (smoothstep(0.01, 0.25, vAmount) - smoothstep(0.24, 0.26, vAmount)) * texture2D(oceanTexture, vUV * 0.0 );\r","\tvec4 sandy = (smoothstep(0.24, 0.27, vAmount) - smoothstep(0.28, 0.31, vAmount)) * texture2D(sandyTexture, vUV * 0.0 );\r","\tvec4 grass = (smoothstep(0.28, 0.32, vAmount) - smoothstep(0.35, 0.40, vAmount)) * texture2D(grassTexture, vUV * 0.0 );\r","\tvec4 rocky = (smoothstep(0.30, 0.50, vAmount) - smoothstep(0.40, 0.70, vAmount)) * texture2D(rockyTexture, vUV * 0.0 );\r","\tvec4 snowy = (smoothstep(0.50, 0.65, vAmount)) * texture2D( snowyTexture, vUV * 10.0 );\r","\tgl_FragColor = vec4(0.0, 0.0, 0.0, 1.0) + water + sandy + grass + rocky + snowy; //, 1.0);\r","}"],"vertex":["uniform sampler2D bumpTexture;\r","uniform float bumpScale;\r","varying float vAmount;\r","varying vec2 vUV;\r","void main() {\r","\tvUV = uv;\r","\tvec4 bumpData = texture2D( bumpTexture, uv );\r","\tvAmount = bumpData.r;\r","\tvec3 newPosition = position + normal * bumpScale * vAmount;\r","\tgl_Position = projectionMatrix * modelViewMatrix * vec4( newPosition, 1.0 );\r","}"]}